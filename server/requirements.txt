fastapi>=0.104
uvicorn[standard]>=0.24
starlette<0.47
pydantic>=2.0
jinja2>=3.1
python-multipart>=0.0.6
itsdangerous>=2.1
# tzdata: required so ``zoneinfo`` can resolve "Asia/Jerusalem" on Windows
# (Linux/macOS already ship an IANA tz database by default).
tzdata>=2024.1
